# Google OAuth secrets — never commit these
client_secret.json
token.json
**/client_secret.json
**/token.json

# Python / PyInstaller build artifacts
windows-app/.venv/
windows-app/build/
windows-app/dist/
windows-app/*.spec
__pycache__/
*.pyc

# Local Claude Code config
.claude/

# Wrangler's local state (account cache, build temp) — machine-specific
worker/.wrangler/

# Electron desktop app — installed deps and anything generated from assets/
desktop-app/node_modules/
desktop-app/build/
desktop-app/dist/
